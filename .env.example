# =============================================================================
# Project Maribel — Environment Variables
# =============================================================================
# Copy this file to .env and fill in all values.
# NEVER commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Meta Platform (Instagram / Facebook)
# -----------------------------------------------------------------------------
# Get these from developers.facebook.com → Your App → Settings → Basic
META_APP_ID=
META_APP_SECRET=

# Long-lived Page Access Token (60-day expiry, auto-refreshed by Workflow 3)
# Generate via Messenger → Instagram Settings → Access Tokens, then exchange
# for long-lived token. See SETUP.md for step-by-step instructions.
META_PAGE_ACCESS_TOKEN=

# Webhook verification token — must match what you enter in Meta App dashboard
# Use any secret string (e.g., eaton_maribel_verify_2026)
META_VERIFY_TOKEN=

# Facebook Page ID linked to your Instagram Business Account
META_PAGE_ID=

# Instagram Business Account ID (NOT your personal IG account)
# Get via Graph API: GET /{page_id}?fields=instagram_business_account
INSTAGRAM_BUSINESS_ACCOUNT_ID=

# -----------------------------------------------------------------------------
# AI — Anthropic (Claude)
# -----------------------------------------------------------------------------
# Get from console.anthropic.com → API Keys
# Used for: conversation intelligence, response generation, summarization
ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# AI — OpenAI (Embeddings)
# -----------------------------------------------------------------------------
# Get from platform.openai.com → API Keys
# Used for: text-embedding-3-small (RAG knowledge base embeddings)
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Supabase — Maribel Agent (dedicated project)
# -----------------------------------------------------------------------------
# New Supabase project created specifically for Maribel
# Get from supabase.com → Project Settings → API
SUPABASE_MARIBEL_URL=
SUPABASE_MARIBEL_ANON_KEY=
SUPABASE_MARIBEL_SERVICE_KEY=

# -----------------------------------------------------------------------------
# Supabase — Eaton Academic (existing project, read-only)
# -----------------------------------------------------------------------------
# Your existing Eaton Academic Supabase project (for student/enrollment lookups)
SUPABASE_EATON_URL=
SUPABASE_EATON_ANON_KEY=

# -----------------------------------------------------------------------------
# Calendly (Appointment Booking)
# -----------------------------------------------------------------------------
# Personal Access Token from calendly.com → Integrations → API & Webhooks
# IMPORTANT: Requires Standard plan or higher for Scheduling API access
CALENDLY_API_KEY=

# -----------------------------------------------------------------------------
# Telegram (Escalation Notifications)
# -----------------------------------------------------------------------------
# Create bot via @BotFather on Telegram. See SETUP.md for instructions.
TELEGRAM_BOT_TOKEN=

# Your personal Telegram chat ID (where escalation alerts are sent)
# Get by messaging the bot, then: GET https://api.telegram.org/bot{TOKEN}/getUpdates
TELEGRAM_CHAT_ID=
